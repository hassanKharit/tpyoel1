{% extends 'base.html.twig' %}

{% block title %}Commentaire{% endblock %}

{% block body %}
    <h1>Commentaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ commentaire.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ commentaire.description }}</td>
            </tr>
            <tr>
                <th>User</th>
                <td>{{ commentaire.user }}</td>
            </tr>
            <tr>
                <th>User_id</th>
                <td>{{ commentaire.userId }}</td>
            </tr>
            <tr>
                <th>Produit_id</th>
                <td>{{ commentaire.produitId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rcommentaire_index') }}">back to list</a>

    <a href="{{ path('app_rcommentaire_edit', {'id': commentaire.id}) }}">edit</a>

    {{ include('rcommentaire/_delete_form.html.twig') }}
{% endblock %}
